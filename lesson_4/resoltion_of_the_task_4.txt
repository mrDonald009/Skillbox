1.
select *
from skill_countries
order by population;

select *
from skill_countries
order by population desc;

select *
from skill_countries
order by continent_id, population desc;

select *
from skill_cities
order by country_id, population desc;

select *
from skill_cities
order by country_id, is_capital desc, population desc;

2.
select *
from skill_cities
order by population desc;

select *
from skill_cities
order by population;

select top 1 *
from skill_cities
where (1=1)
	and country_id = 10
order by country_id, population;

select top 1 *
from skill_cities
where (1=1)
	and country_id = 4
order by country_id, population desc;

select *
from skill_cities
where (1=1)
	and is_capital = 1
order by population desc;

3.
select
	 first_name
	,length(first_name) as len_first_name
	,trim(first_name) as trim_first_name
	,length(trim(first_name)) as len_trim_first_name
from skill_customers;

select
	*
from skill_customers
where (1=1)
	and length(first_name) - length(trim(first_name)) = 0;

select
	*
from skill_customers
where (1=1)
	and length(first_name) like length(trim(first_name));

select
	ltrim(rtrim(first_name)) as first_name
       ,ltrim(rtrim(last_name)) as last_name
from skill_customers;

4.
select
	 concat(upper(substring(item_name,1,1)), substring(item_name,2, len(item_name) - 1)) as item_name
	,price_rub
	,cnt
from skill_price;

select 
	 str_columns
	,trim(substring(str_columns, 1, 3)) as id
	,trim(substring(str_columns, 5, 8)) as first_name
	,trim(substring(str_columns, 14, 13)) as last_name
	,trim(substring(str_columns, 28, 8)) as age
	,trim(substring(str_columns, 37, 1)) as gender
from skill_fix where not trim(substring(str_columns, 28, 8)) like '% %';

select 
	 str_columns
	,trim(substring(str_columns, 1, 3)) as id
	,trim(substring(str_columns, 5, 8)) as first_name
	,trim(substring(str_columns, 14, 13)) as last_name
	,replace(trim(substring(str_columns, 28, 8)), ',','') as age
	,trim(substring(str_columns, 37, 1)) as gender
from skill_fix;

5.
MS SQL:
select 
	last_name
from skill_customers
where (1=1)
	and last_name like '%[wrong]%' escape '[';

select
	last_name
from skill_customers
where (1=1)
	and charindex('[wrong]', last_name) > 0;

MySQL:
select 
	last_name
from skill_customers
where (1=1)
	and locate('[wrong]', last_name) > 0;

PostgreSQL:
select
	last_name
from skill_customers
where (1=1)
	and strpos(last_name, '[wrong]') > 0;
----
MS SQL:
select
	 customer_id
	,first_name
	,concat(substring(trim(last_name),0,charindex('[w', trim(last_name))), substring(trim(last_name), charindex('[w', trim(last_name)) + 7, len(trim(last_name)) -  charindex('[w', trim(last_name)) + 7)) as last_name
	,age
	,gender
from skill_customers
where (1=1)
	and charindex('[wrong]', last_name) > 0;

ver.0.02
select
	 customer_id
	,first_name
    	,concat(left(trim(last_name), charindex('[w', trim(last_name)) - 1), right(trim(last_name), len(trim(last_name)) - charindex('g]', trim(last_name)) - 1)) as last_name
    	,age
	,gender
from skill_customers
where (1=1)
	and charindex('[wrong]', last_name) > 0;

MySQL:
select
	 customer_id
	,first_name
	,concat(substring(trim(last_name), 1, locate('[w', trim(last_name)) - 1), substring(trim(last_name), locate('[w', trim(last_name)) + 7, length(trim(last_name)) - locate('[w', trim(last_name)) + 7)) as last_name
	,age
	,gender
from skill_customers
where (1=1)
	and locate('[wrong]', last_name) > 0;

ver.0.02
select
	 customer_id
	,first_name
	,concat(left(trim(last_name), locate('[w', trim(last_name)) - 1), right(trim(last_name), length(trim(last_name)) - locate('g]', trim(last_name)) - 1)) as last_name
	,age
	,gender
from skill_customers
where (1=1)
	and locate('[wrong]', last_name) > 0;

PostgreSQL:
select
	 customer_id
	,first_name	
	,concat(substring(trim(last_name), 0, strpos(trim(last_name), '[w')), substring(trim(last_name), strpos(trim(last_name), '[w') + 7, length(trim(last_name)) - strpos(trim(last_name), '[w') + 7)) as last_name
	,age	
	,gender
from skill_customers
where (1=1)
	and strpos(last_name, '[wrong]') > 0;

ver.0.02
select
	 customer_id
	,first_name	
	,concat(left(trim(last_name), strpos(trim(last_name), '[w') - 1), right(trim(last_name), length(trim(last_name)) - strpos(trim(last_name), 'g]') - 1)) as last_name
	,age	
	,gender
from skill_customers
where (1=1)
	and strpos(last_name, '[wrong]') > 0;
----
MS SQL:
select
	 customer_id
	,first_name
	,replace(trim(last_name), '[wrong]', '') as last_name
	,age
	,gender
from skill_customers
where (1=1)
	and trim(last_name) like '%[wrong]%'
	and charindex('[wrong]', last_name) > 0;

MySQL:
select
	 customer_id
	,first_name
	,replace(trim(last_name), '[wrong]', '') as last_name
	,age
	,gender
from skill_customers
where (1=1)
	and trim(last_name) like '%[wrong]%';

PostgreSQL:
select
	 customer_id
	,first_name
	,replace(trim(last_name), '[wrong]', '') as last_name
	,age
	,gender
from skill_customers
where (1=1)
	and trim(last_name) like '%[wrong]%';